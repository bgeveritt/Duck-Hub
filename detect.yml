---
platform: linux

image_resource:
  type: docker-image
  source: 
    repository: jvalance/docker-java-maven

inputs:
- name: duck-hub

run:
  path: bash
  args: 
  - -exc
  - |
    cd duck-hub
    mvn package
    echo $PATH
