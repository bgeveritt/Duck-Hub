---
platform: linux

image_resource:
  type: docker-image
  source: 
    repository: openjdk
    tag: '8'

inputs:
- name: duck-hub

caches:
- path: duck-hub/target

run:
  path: bash
  args: 
  - -exc
  - |
    cd duck-hub
    ls
    pwd
    bash <(curl -s https://blackducksoftware.github.io/hub-detect/hub-detect.sh) --blackduck.hub.url=https://hubeval68.blackducksoftware.com --blackduck.hub.username=sysadmin --blackduck.hub.password=F@w0m0! blackduck.hub.auto.import.cert=true
