---
platform: linux

image_resource:
  type: docker-image
  source: 
    repository: openjdk
    tag: '8'

inputs:
- name: mvn-package-output

run:
  path: sh
  args: 
  - -exc
  - |
    ls
    cd duck-hub
    ls